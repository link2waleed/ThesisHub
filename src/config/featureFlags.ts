// ── Feature Flags ──
// Toggle modules on/off without removing code.
// Set to `true` to re-enable the Thesis Ideas module.

export const featureFlags = {
    /** TEMPORARILY DISABLED: Thesis Ideas module (client request). Will be re-enabled in a future phase. */
    ENABLE_THESIS_IDEAS: false,
} as const;
